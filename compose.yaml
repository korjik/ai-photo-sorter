services:
  ai-photo-sorter:
    build:
      context: .
    image: ai-photo-sorter:latest
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      PHOTO_ROOT: /photo
      SOURCE_ROOTS: ${SOURCE_ROOTS:-Unsorted/SourceA:Unsorted/SourceB}
      DESTINATION: ${DESTINATION:-AI-sorted}
    volumes:
      - ${HOST_PHOTO_ROOT:?Set HOST_PHOTO_ROOT to the host photo library root}:/photo
      - ./ai-photo-sorter.config.json:/app/ai-photo-sorter.config.json:ro
    command: ["--config", "/app/ai-photo-sorter.config.json", "--dry-run"]
